apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ${{values.bucket_name}}
  namespace: argocd
spec:
  project: ${{values.project_name}}
  source:
    repoURL: https://${{values.repo_url}}.git
    targetRevision: HEAD
    path: .
  destination:
    server: https://kubernetes.default.svc
    namespace: ${{values.project_name}}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true